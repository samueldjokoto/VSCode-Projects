// Lifetime supply calculator 

// function calculateSupply (age, amountPerDay){
// const maxAge = 120;
// totalAmount = (amountPerDay * 365) * (maxAge - age);
// const message = 'You will need ' + totalAmount + ' wings to last you until the ripe old age of ' + maxAge;
// console.log(message);
// }


// calculateSupply(28, 36);
// calculateSupply(28, 2.5);
// calculateSupply(28, 400);


// The Geometrizer

// function calc (radius){
//         let circumfrence = Math.PI * 2*radius;
//         console.log('The cirucmfrence is' + circumfrence);
//         let area = Math.PI * radius*radius;
//         console.log("The area is " + area);
// }

// calc();

//The temp

// function celciusToFahrenheit(celcius){
//         celcius = 20;
//         cInfahrenheit = (celcius / 5) * 9 + 32;
//         const message = celcius + ' is ' + cInfahrenheit;
//         console.log(message);
// }

// celciusToFahrenheit();

// function celsiusToFahrenheit(celsius) {
//   celsius = 20;
//   var celsiusInF = (celsius*9)/5 + 32;
//   console.log(celsius + '°C is ' + celsiusInF + '°F');
// }

// celsiusToFahrenheit();


// LOOPS

// const colours = ['red', 'yellow', 'blue', 'black', 'green', 'purple'];
// const list = [1, 2, 3, 4, 5, 6];
// let text = "";

//   for (let i = 0; i < colours.length, list.length; i++){
//     if (i === 6) {break;}
//       console.log('colour ' + list[i] + ' is ' + colours[i] );
//   }


let i = 2;

// for( let i = 0; i < evens; i ++){
//   if (i === 30) {break;}
//   console.log(evens);
// }

while( i < 30) {
  console.log(i);
  i += 2;
}
